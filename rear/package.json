{
  "name": "newssystem",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "json-server ./db.json --watch --port 3001 --host 127.0.0.1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
